import type {Metadata} from "next";
import "./creators.css";
import "./creator-enhancements.css";
export const metadata:Metadata={title:"Formie Creator Portal",robots:{index:false,follow:false,nocache:true}};
export default function Layout({children}:{children:React.ReactNode}){return children}
