import type { FunnelStep } from "@/lib/admin/dashboard-data"; import { value } from "./dashboard-format";
export function DashboardFunnel({ funnel }: { funnel: FunnelStep[] }) { return <section className="admin-panel admin-funnel-panel"><div className="admin-panel-heading"><div><span className="admin-kicker">Ordered conversion</span><h2>Acquisition to repeat analysis</h2></div><p>Transitions require ordered events. Immature cohorts are excluded from time-bound denominators.</p></div><div className="admin-funnel">{funnel.map((step, index) => <article key={step.key}><div className="admin-funnel-top"><span>{String(index + 1).padStart(2,"0")}</span><b>{value(step.users)}</b></div><h3>{step.label}</h3><div className="admin-funnel-track"><i style={{ width: `${Math.max(0, Math.min(100, step.conversion.value ?? 0))}%` }}/></div><small>{value(step.conversion,"percent")} · median {value(step.medianTransitionMs,"duration")}</small></article>)}</div></section>; }
