import type { BreakdownRow } from "@/lib/admin/dashboard-data"; import { value } from "./dashboard-format";
function Table({ title, rows }: { title: string; rows: BreakdownRow[] }) { const keys = [...new Set(rows.flatMap((row) => Object.keys(row.metrics)))]; return <section className="admin-panel"><div className="admin-panel-heading"><div><span className="admin-kicker">Segmentation</span><h2>{title}</h2></div></div><div className="admin-table-wrap"><table><thead><tr><th>Group</th>{keys.map((key) => <th key={key}>{key.replaceAll("_"," ")}</th>)}</tr></thead><tbody>{rows.length ? rows.map((row) => <tr key={row.key}><td><b>{row.label}</b></td>{keys.map((key) => <td key={key}>{row.metrics[key] ? value(row.metrics[key]!, key.toLowerCase().includes("rate") ? "percent" : "count") : "—"}</td>)}</tr>) : <tr><td className="admin-empty" colSpan={Math.max(1, keys.length + 1)}>No eligible data yet.</td></tr>}</tbody></table></div></section>; }
export function DashboardBreakdowns({ helpfulness, loop }: { helpfulness: BreakdownRow[]; loop: BreakdownRow[] }) { return <div className="admin-tables"><Table title="Usefulness by exercise, model, and analysis type" rows={helpfulness}/><Table title="Next-set behavior after feedback" rows={loop}/></div>; }
