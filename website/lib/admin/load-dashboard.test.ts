import assert from "node:assert/strict"; import test from "node:test"; import { AdminAccessError, loadAdminDashboard } from "./load-dashboard"; import { rawDashboardSnapshot } from "./dashboard-test-fixture";
test("authorizes the founder before the privileged filtered query", async()=>{let calls=0; const result=await loadAdminDashboard({getAuthenticatedEmail:async()=>"FOUNDER@example.com",getSnapshot:async(filters)=>{calls++;assert.deepEqual(filters,{window:"7d",exerciseId:42});return rawDashboardSnapshot;}},"founder@example.com",{window:"7d",exerciseId:42});assert.equal(result.snapshot.headline.newSignups.value,42);assert.equal(calls,1);});
test("does not query for missing or non-founder sessions",async()=>{let calls=0;for(const email of [null,"other@example.com"])await assert.rejects(loadAdminDashboard({getAuthenticatedEmail:async()=>email,getSnapshot:async()=>{calls++;return rawDashboardSnapshot;}},"founder@example.com"),(error)=>error instanceof AdminAccessError);assert.equal(calls,0);});
